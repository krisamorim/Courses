/*
var - accessible in other block
let - accessible in the same block
const - the value doesn't change

- can be composed by letters, numbers and the symbols $ and _ (underscore);
- Can't beginning with number;
- can't contain signs like  ` ~ ^
- it is case-sensitive 


Example:

var yearsOld = 12;
var studentName = "Name";
*/

