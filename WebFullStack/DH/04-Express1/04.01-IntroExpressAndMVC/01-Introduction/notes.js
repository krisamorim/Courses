/*
1-
to create the folder for a node project
inside the folder. type it:
npm init -y para gerar o package json

2-
install. type it:
npm install express --save

3-
to create entry point. Type it: 
touch app.js

4-to create a constant, in order to import the express inside to the app.js. Type it:
const express = require('express');

5-
instantiate an express function using the constant created previously to load express resources into the project. Type it:
let app = express();


*/

