//within this framework there are functionalities ready to use

//express() -> when run return an object

app.METHOD(); //what is the http method from request
app.get(); // 
app.post();
app.put();
app.patch(); //where came from
app.delete();
//the methods above give acces to two objects: request and response

response.send(); //allow send a string or array as an answer, withou define type
